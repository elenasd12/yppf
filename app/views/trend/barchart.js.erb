$('.search_results').html("<%= escape_javascript (render(partial: 'search_results')) %>");

//<!--High Charts bar chart  -->
$(function () {
    $('#bar').highcharts({
        chart: {
            backgroundColor: '#ffffe6',
            type: 'column'
        },
        title: {
            text: ''
        },
        xAxis: {
            categories: <%= @months.to_json.html_safe %>
        },
        yAxis: {
            title: {
                text: 'Money Spent'
                // y axis - how much money
            }
        },
        series: [{
            // months
            name: '<%= @category_name %>',
            data: <%= @values.to_json.html_safe %>,
            color: '#ED6A5A'
        }]
    });
});
