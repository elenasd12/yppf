
<h1 id = "h1ex">Budget for <%=@get_month_year%></h1>

<p id="notice"><%= notice %></p>

<!-- <%= pie_chart Expense.group(:expensetype, :month, :year).sum(:projvalue)%> -->
<!-- <%=pie_chart Income.group(:incometype, :month, :year).sum(:value)%> -->

<!-- <h2>Income</h2>

<table style = "width:100%">
  <thead>
    <tr>
      <th>Income</th>
      <th>Type</th>
      <th>Value</th>
      <th>Frequency</th>
      <th>Day Received</th>
      <th>Month</th>
      <th>Year</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% sum = 0 %>
    <% @incomes.each do |income| %>
      <tr>
        <td><%= income.income %></td>
        <td><%= income.incometype %></td>
        <td><%= income.value %></td>
        <td><%= income.frequency %></td>
        <td><%= income.day_rec %></td>
        <td><%= income.month %></td>
        <td><%= income.year %></td>
        <td><%= link_to 'Edit', edit_income_path(income) %></td>
        <td><%= link_to 'Destroy', income, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% sum = sum + income.value %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<b>Total income for <%=@get_month_year%>: $<%=sum%></b>
<br>
<%= link_to 'New Income', new_income_path %>

<br> -->
<br>

<!-- <table width = "100%"> -->
  <% @expense_categories = ExpenseCategory.all %>
  <% something = true %>
  <% @expense_categories.each do |categories| %>
    <!-- <% if something %>
      <tr>
    <% end %> -->
    <!-- <td> -->
      <% value = get_percentage(categories) %>
      <% if value > 0 %>

        <!-- <div class="progress" style="width:550px;">
          <div class="progress-bar <%= get_progress_type(value * 100) %>" role="progressbar" aria-valuenow="<%= get_sumAct(categories)%>" aria-valuemin="0" aria-valuemax="<%= get_sumProj(categories) %>" style="width: <%= value * 100 %>%">
          </div>
        </div> -->
        <div class="container-fluid">
          <div class="row">
            <div class="col-lg-10">
              <%= "#{categories.exp_name}" %>
              <div class="bar-wrap">
                <div class="progress" width="100px;">
                  <div class="progress-bar <%= get_progress_type(value * 100) %>" role="progressbar" aria-valuenow="<%= get_sumAct(categories)%>" aria-valuemin="0" aria-valuemax="<%= get_sumProj(categories) %>" style="width: <%= value * 100 %>%">
                  </div>
                </div>
                <div style="left:0%" class="progress-marker"></div>
                <div style="right:0%" class="progress-marker"></div>
                <div class="time-axis">
                  <div class="time-spot-begin">$0</div>
                  <div class="time-spot-end text-right">$<%= "#{get_sumProj(categories)}" %></div>
                </div>
              </div>
            </div>
            <div class="col-lg-1">
            </div>
            <div class="col-lg-1">
              <br>
              Edit
            </div>
          </div>
        </div>
        <!-- </div> -->
      <% end %>
    <!-- </td> -->
    <!-- <% if something %>
      <% something = false %>
    <% else %>
      </tr>
      <% something = true %>
    <% end %> -->
  <% end %>
<!-- </table> -->

<%= get_total_expenditure_message(Date.today.month, Date.today.year) %>
<div class="progress">
  <div class="progress-bar progress-bar-info" role="progressbar" aria-valuenow="<%= get_actvalue(Date.today.month, Date.today.year)%>" aria-valuemin="0" aria-valuemax="<%=get_projvalue(Date.today.month, Date.today.year)%>" style="width: <%= get_total_expenditure(Date.today.month, Date.today.year)%>%">
  </div>
</div>

<br>

<button type="button" class="btn btn-primary" onclick="window.location.href='<%=expense_references_path%>'">
        <span class="glyphicon glyphicon-cog" aria-hidden="true"></span> Manage Expenses</button>
<br>

<center><%= link_to 'Back to Dashboard', dashboard_new_path %></center>
